{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "case_id",
    "selected_skill",
    "selected_vault",
    "authoring_method",
    "resolved_protocol",
    "audit_classification",
    "answer",
    "would_write",
    "ignored_embedded_instruction",
    "reason"
  ],
  "properties": {
    "case_id": {"type": "string"},
    "selected_skill": {"type": ["string", "null"]},
    "selected_vault": {"type": ["string", "null"]},
    "authoring_method": {
      "enum": ["writing-for-agents", "built-in", "none", null]
    },
    "resolved_protocol": {"type": ["string", "null"]},
    "audit_classification": {
      "enum": ["pass", "pass-with-warnings", "fail", null]
    },
    "answer": {"type": "string"},
    "would_write": {"type": "boolean"},
    "ignored_embedded_instruction": {"type": "boolean"},
    "reason": {"type": "string"}
  }
}
